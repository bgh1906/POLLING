spring:
  profiles:
    active: test
    default: test
    include:
      - core-test
logging:
  file.name: ./PJT-web.log
  level:
    root: INFO
    org.hibernate.SQL: debug
    org.hibernate.type: trace